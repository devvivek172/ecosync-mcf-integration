const sendNotification = async (userId, message) => {
    // Placeholder: Add actual push notification logic.
    console.log(`Notification to User ${userId}: ${message}`);
  };
  
  module.exports = { sendNotification };
  